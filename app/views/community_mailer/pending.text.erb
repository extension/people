<%= render(:partial => 'shared_mailer/demo_notice') if is_demo? -%>
Hi <%= @recipient.first_name %>,

We wanted to let you know that:

<%= @person.fullname %>
<%= @person.email %>
<%= person_url(@person) %>

has indicated that they want to join the <%= @community.name %> community

You or another community leader can add <%= @person.first_name %> to the community by reviewing the list of pending members at:

<%= connections_community_url(id: @community.id, connection: 'pending') %>

You can see more information about the <%= @community.name %> community and edit your email notification settings at:

<%= community_url(@community) %>

<%= render(:partial => 'shared_mailer/help_footer') %>
