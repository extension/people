<%= render(:partial => 'shared_mailer/demo_notice') if is_demo? -%>
Hi <%= @recipient.first_name %>,

We are sending this confirmation email to verify that your email address is correct so that you can set a new password for your account.

To confirm your email address and set a new password, please click the following link:

<%= set_password_url(token: @recipient.reset_token) %>

<%= render(:partial => 'shared_mailer/help_footer') %>
