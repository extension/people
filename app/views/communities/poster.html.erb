<%- @page_title = "#{@community.name}" -%>

<style media="screen" type="text/css">
  .community-masthead {
    background: transparent url(<%= asset_path 'community-masthead-placeholder.png' %>) center 50% no-repeat;
  }
</style>

<div class="page-header">
  <div class="community-masthead">
    <h2><%= @page_title %></h2>
  </div>
  <p class="community-public-description"><%= description_for_community(@community) -%></p>
  <p><%= link_to("View this community", community_path(@community), class: 'btn btn-default btn-sm') %></p>
</div>

<div class="row">
  <%- i = 0 -%>
  <%- for person in @connections -%>
    <%- i += 1 -%>
    <div class="col-md-4 col-sm-6 col-xs-6">
      <div class="person-profile">
        <div class="person-profile-avatar"><%= link_to(person_avatar(person, {image_size: :large}), person_path(person)) %></div>
        <h4 class="person-profile-name"><%= link_to("#{person.fullname}", person_path(person)) %></h4>
        <p class="person-profile-title"><%= person.title %></p>
        <div class="person-profile-social-icons">
          <%- person.facebook_connections.each do |fbc| -%>
            <%- if current_person or fbc.is_public? -%>
              <%- url = social_network_url(fbc) || "https://www.facebook.com/public?query=#{fbc.accountid}" # this should really be a helper -%>
              <a href='<%= url -%>' title='<%= fbc.accountid -%>'
              <span class="fa-stack fa-lg fa-gray">
                <i class="fa fa-circle fa-stack-2x"></i>
                <i class="fa fa-facebook fa-stack-1x fa-inverse"></i>
              </span>
              </a>
            <%- end -%>
          <%- end -%>

          <%- person.twitter_connections.each do |tc| -%>
            <%- if current_person or tc.is_public? -%>
              <%- url = social_network_url(tc) || "https://twitter.com/#{tc.accountid}" # this should really be a helper -%>
              <a href='<%= url -%>' title='<%= tc.accountid -%>'
                <span class="fa-stack fa-lg fa-gray">
                  <i class="fa fa-circle fa-stack-2x"></i>
                  <i class="fa fa-twitter fa-stack-1x fa-inverse"></i>
                </span>
              </a>
            <%- end -%>
          <%- end -%>
        </div>
      </div>
    </div>
    <%- if i % 3 == 0 -%>
    <div class="clearfix visible-lg-block visible-md-block"></div>
    <% end %>
    <%- end -%>

</div>
